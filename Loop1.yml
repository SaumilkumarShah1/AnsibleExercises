- hosts: all
  vars:
    loop_1: "hello1"
  tasks:
  - name: Prepare Array for ArrayList
    set_fact:
      ArrayList: []
      
  - name: Ansible loop with conditional example
    set_fact:
        my_joined_list: "{{ item }}"
    with_items:
      - "hello1"
      - "hello2"
      - "hello3"
      - "hello5"
    when: item == "{{ loop_1 }}" && item == "hello2"

        
  - name: Examine the joined string
    debug:
        msg: "{{ my_joined_list }}"
